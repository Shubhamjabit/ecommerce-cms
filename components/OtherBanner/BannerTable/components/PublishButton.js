import React from 'react';
import {Row, Button} from 'antd';
import styles from './styles.module.scss';

const PublishButton = () => {
  return <Button className={styles.publishButton}>Published</Button>;
};

export default PublishButton;
